source "https://rubygems.org"

# Use a compatible version of Jekyll
gem "jekyll", "~> 4.2.0"

# Use the latest compatible version of github-pages
gem "github-pages", "~> 226", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.12"
end

